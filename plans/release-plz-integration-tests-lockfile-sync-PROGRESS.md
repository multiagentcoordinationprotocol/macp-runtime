# PROGRESS — release-plz keeps `integration_tests/Cargo.lock` in sync

Plan: [`plans/release-plz-integration-tests-lockfile-sync.md`](release-plz-integration-tests-lockfile-sync.md)
Branch: TBD, cut from `main` @ `0bba3a8` (2026-09-06)
Model tiering for this run: **Opus plans, Opus executes, fresh Opus verifies** — user
override of `/implement`'s default Sonnet executor, and an explicit **no-Fable** instruction:
where the skill would escalate to Fable for a one-way door, a fresh Opus agent is used and
the substitution is noted. (No phase in this plan is in fact a one-way door.)

## Status

| Phase | Title | Status | Verdict | Rounds | Commit |
|-------|-------|--------|---------|--------|--------|
| 0 | Plan + reverify | DONE | FLAWED -> patched (rev 2) | 1 | (uncommitted) |
| 1 | Sync the lock on the release PR branch | DONE | GAPS -> closed (round 2) | 2 | `a7b6efc` (PR #155) |
| 2 | Extend the CI lockfile guard | DONE | GAPS -> closed (round 2) | 2+1 | `c9650df` (PR #156) |
| 3 | Document the invariant, retire the chore | DONE | GAPS -> PASS (round 2) | 2 | `95ea207` (PR #157) |

## Repo map

Written during planning so `/implement` does not re-scan the repo.

### The files this plan changes

| Path | Purpose |
|------|---------|
| `.github/workflows/release-plz.yml` | release-plz job (`push: main`). Runs the action with `id: release-plz`, then calls `publish.yml` via `workflow_call` gated on `releases_created`. **Phase 1 adds a separate `sync-integration-lock` job here** — a step inside the `release-plz` job would gate `publish` on its own success (finding S3), so the sync must be its own job. |
| `.github/workflows/ci.yml` | The **Check (MSRV)** job holds the `cargo metadata --locked` guard added by PR #150, currently root-only with a comment explaining why. **Phase 2 adds the `integration_tests` guard to the `integration` job instead** (plan rev 2, §Phase 2) — `check` pins the MSRV toolchain, and the second lock must be validated by the same cargo that builds it. Phase 2 also updates the root-only comment in `check`. |
| `CLAUDE.md` | "Releasing to crates.io" section. **Phase 3.** NOTE: gitignored in this tree — edits are local-only and will not appear in the PR diff. |
| `docs/testing.md` | Integration-test docs. **Phase 3** adds the contributor-facing lock rule. |

### The files this plan reasons about but does NOT change

| Path | Why it matters |
|------|----------------|
| `integration_tests/Cargo.toml` | `macp-runtime = { path = ".." }` — path-only, **no version requirement**. This is the mechanism: the lock records a version the manifest never constrains. |
| `integration_tests/Cargo.lock` | The artifact that goes stale. Regenerated by Phase 1's workflow step, guarded by Phase 2. |
| `Cargo.toml` | `[workspace] exclude = ["integration_tests"]` — the deliberate separation that creates the second lock. `[workspace.package] version` is what release-plz bumps. |
| `release-plz.toml` | `publish = false`, `semver_check = true`, seven packages in one `version_group = "macp"`. Verified: **no hook runs at release-PR-preparation time**, which is why the fix lives in the workflow. |
| `.github/workflows/publish.yml` | `cargo publish --workspace --locked` — root workspace only, so `integration_tests` never reaches it. Must not be disturbed. |
| `.github/dependabot.yml` | Has a `cargo` entry for `/integration_tests`, so dependabot opens its own PRs against that lock. Phase 1 must not fight it — hence `cargo metadata`, not `cargo update`. |

### Prior art / evidence this is recurring

| Path | What it records |
|------|-----------------|
| `plans/rfc-macp-0013-commitment-hash-PROGRESS.md:61` | lock found stale at 0.5.0 vs 0.6.1; fixed as housekeeping commit `d3c0d66` |
| `plans/list-sessions-pagination-PROGRESS.md:60` | "Separate crate outside the workspace; dependabot never updates it. Check staleness before pushing." |
| `plans/list-sessions-pagination.md:451` | ship-checklist step 7: check the lock for staleness before pushing |

### Conventions to follow

- `ASSUMPTIONS.md` entries: `## Title` then `- **Plan:** / **Assumed:** / **Chose:** /
  **Alternatives:** / **Blast radius if wrong:** / **Status:**`. Currently **0 UNCONFIRMED**.
- `DECISIONS.md` is the durable record `/reconcile` appends to.
- Workflow edits are validated locally with `actionlint` (installed) plus a `yaml.safe_load`
  parse.
- **Environment:** every cargo command must be prefixed `RUSTC_WRAPPER=""` — the repo's
  `.cargo/config.toml` sets `rustc-wrapper = "sccache"` and the daemon is broken in this
  sandbox. Never run `sccache --stop-server` or `pkill`.

## Phase 0 — planning + reverification

A fresh **Opus** reverifier pressure-tested revision 1 against the real code and against live
workflow-run logs. Verdict: **FLAWED**. It returned 2 BLOCKERs, 6 SHOULD-FIXes, 6 NITs; all are
applied in revision 2 of the plan. The material ones:

| ID | Finding | Applied as |
|----|---------|-----------|
| B1 | The action emits the literal string `{}` (not empty) when no PR is produced, so an `if: … .pr != ''` guard runs anyway and fails the job | Guard on `prs_created == 'true'` |
| B2 | release-plz **closes and recreates** the release PR if any extra commit's author login is not `…[bot]` — permanent PR churn | Commit identity set to `github-actions[bot]` explicitly, with the mechanism documented |
| S3 | Sync as a *step* in the `release-plz` job would skip `publish` on failure — reproducing the 0.6.1 tag-without-publish incident | Sync moved to its **own job**; `publish` untouched |
| S4 | Phase 1's assertion silently depended on Phase 2; case-(b) staleness (`d3c0d66`: 19+/57-) is not version-lines-only | Assertion rescoped to "no shared third-party pin changes"; revert-both-or-neither stated |
| S5 | `integration_tests/Cargo.lock:3` is `version = 4`, the lock **format** version — a `version = ` match is both too loose and too brittle | Assertion compares name→version maps, not line patterns |
| S6 | PR #144 is live *right now* with exactly this staleness | Named as the first real subject; Phase 1 must precede Phase 2 |
| S7 | The push creates a **second** `action_required` run at a new head SHA; approving the wrong one leaves the PR unmergeable | Job logs the new head SHA; documented in Phase 3 |
| S8 | `CLAUDE.md` is gitignored and **no tracked file documents the release process** | Phase 3 retargeted at `CONTRIBUTING.md` + `docs/testing.md` |

It also **verified** the load-bearing claim independently: simulating the exact release-plz
bump, `cargo metadata` changed **14 lines, all `version = "…"`**, worked `--offline`, and did
**not** touch the root `Cargo.lock`. Re-run against PR #144's real head (`d90439e`): 7+/7-.

Model note: the reverifier ran on **Opus**, not Fable, per the user's explicit no-Fable
instruction. No phase in this plan is a one-way door, so no tier-3 escalation was owed anyway.

## Checkpoints

_(appended per phase by `/implement`)_

### Phase 1 — sync the lock on the release PR branch

**Status: DONE.** Branch `ci/sync-integration-tests-lockfile`, commit **`d24d7dc`**, shipping
as its **own PR** (not folded into a later phase). Two rounds: round 1 shipped `bb7a4bb`, a
fresh **Opus** verifier returned **GAPS**, round 2 closed every item and amended into `d24d7dc`.
Files touched: `.github/workflows/release-plz.yml` and this PROGRESS file. Nothing else.

#### What shipped

A new `sync-integration-lock` **job** in `.github/workflows/release-plz.yml` — deliberately a
job and not a step, per finding S3. Gated on `prs_created == 'true'` (B1). It checks out the
release PR by number, regenerates `integration_tests/Cargo.lock` with `cargo metadata`,
asserts that only the internal crate pins moved, verifies the repair with
`cargo metadata --locked`, and commits + pushes as `github-actions[bot]` (B2), skipping
entirely when there is nothing to commit.

#### Round 1 verdict: GAPS

The verifier passed 7 of 9 acceptance criteria and all workflow-level checks (YAML parse,
`actionlint`, job/step wiring, `publish` untouched, `prs_created` guard, bot identity,
no-empty-commit path). It returned 9 gaps, all in or around the assertion step:

| ID | Gap | Closed by |
|----|-----|-----------|
| G1 | **BLOCKER.** `pkgs[name] = (version, src)` keys on NAME only. The lock has 346 `[[package]]` blocks but 310 distinct names — 25 names resolve at 2-4 versions at once. Last-block-wins on an ascending-sorted file means only the **highest** version of a duplicated name is ever compared, so re-pinning a lower duplicate passed as "OK" (safety property failing **open**) | Comparison rebuilt over `name -> SET of versions` |
| G2 | The same dict fails **closed** on a legitimate removal: dropping the `base64 0.23.1` block made the map read `0.23.1 -> 0.22.1` and exit 1, though the plan explicitly permits removals | Same fix: a version disappearing with none appearing is a removal, permitted. A violation requires a version to disappear **and** a different one to appear for the same name |
| G3 | A 0-byte or header-only lock exited 0 — every package read as removed, and removals are permitted — and would have been committed and force-pushed onto the release PR | Floor check: the lock must parse to ≥ 50 package blocks **and** contain all seven internal crates plus `macp-integration-tests` |
| G4 | A `[[package]]` block missing its `version` key was skipped by `if n and v:` and so read as a *removal* | Malformed input now fails loudly with the block index |
| G5 | The source-less exemption `not (osrc or nsrc)` was correct but undocumented as to *what* it covers | Comment now names `macp-integration-tests 0.0.0` explicitly and records the residual: a third-party crate `[patch]`ed to a path would silently gain the exemption |
| G6b | Push target derived from `git rev-parse --abbrev-ref HEAD`. Works for same-repo PRs; for a fork `gh pr checkout` names the branch `<owner>-<branch>` and the push would create a **new branch** instead of updating the PR | Push to `fromJSON(needs.release-plz.outputs.pr).head_branch`, with an empty-value guard |
| G7 | No `timeout-minutes`; the job inherits the 360-minute default while holding the `release-plz-<ref>` concurrency group | `timeout-minutes: 15` |
| M1 | The step only proved nothing *bad* moved; it never proved the sync **worked** | Added a `cargo metadata --locked --manifest-path integration_tests/Cargo.toml` step — `--locked` makes cargo refuse to write, so a partial or no-op repair fails |
| M4 | `git add integration_tests/Cargo.lock` would leave a modified root `Cargo.lock` dirty and silently dropped | `git status --porcelain -- Cargo.lock` asserted empty after regeneration |

The two proven round-1 false negatives, reproduced against the shipped `bb7a4bb` script:
`base64 0.22.1 -> 0.22.2` exit **0** "OK", and `thiserror` + `thiserror-impl`
`1.0.69 -> 1.0.70` exit **0** "OK". The proven false positive: removing `base64 0.23.1`
exit **1**. Also exit 0 under `bb7a4bb`: an empty lock, and a `tokio` block with its
`version =` line stripped.

#### Round 2 regression evidence

The assertion script was extracted from the **shipped workflow file** via `yaml.safe_load`
(not hand-copied), so these exercise the exact bytes the runner sees. All 13 cases behaved as
required.

| Case | Required | Actual |
|------|----------|--------|
| D — `base64 0.22.1 -> 0.22.2` (lower duplicate) | now exit 1 | **exit 1**, `- base64 0.22.1 -> 0.22.2` |
| D2 — `thiserror` + `thiserror-impl` `1.0.69 -> 1.0.70` | now exit 1 | **exit 1**, both entries listed |
| E — remove `base64 0.23.1` (higher duplicate) | now exit 0 | **exit 0**, `removed version: base64 0.23.1` |
| Control — `-nanoid +ryu +serde_urlencoded` | stay exit 0 | **exit 0**, added/removed reported |
| T1 — internal `0.7.3 -> 0.7.4` | stay exit 0 | **exit 0**, 7 `internal bump:` lines |
| T2 — `tokio` pin tampered | stay exit 1 | **exit 1** |
| T3 — real `d3c0d66` (`d3c0d66^` vs `d3c0d66`) | stay exit 0 | **exit 0**; `+sha2`, `-ryu -serde_urlencoded`, 7 internal bumps, and `removed version: reqwest 0.12.28` — a *second* reqwest version the name-keyed map had hidden entirely |
| T4a — format `4 -> 5` alone | exit 0 with note | **exit 0**, `note: lockfile format version 4 -> 5` |
| T4b — format `4 -> 5` **and** tokio tampered | exit 1 | **exit 1**, note printed, violation still caught |
| T5 — `macp-proto 0.1.9 -> 9.9.9` | stay exit 1 | **exit 1** (registry crate, not exempt) |
| G3a — 0-byte lock | now exit 1 | **exit 1**, "only 0 package blocks (expected at least 50)" |
| G3b — header-only lock | now exit 1 | **exit 1**, same message |
| G4 — `tokio` block, `version =` stripped | now exit 1 | **exit 1**, "`[[package]]` block #237 is malformed (name='tokio', version=None)" |

End-to-end with real cargo, simulating release-plz's bump (`[workspace.package] version`
`0.7.3 -> 0.7.4`, `cargo metadata --offline`):

- diff is **7 insertions / 7 deletions**, every line a `version = "0.7.3"` -> `"0.7.4"`
- root `Cargo.lock` **sha256 byte-identical** before and after; `git status --porcelain -- Cargo.lock` empty (M4 holds)
- `cargo metadata --locked` **fails** on the stale lock beforehand and **succeeds** after the
  repair (M1 is a real positive assertion, not a tautology)
- the shipped assertion run against the real regenerated lock: exit 0, 7 `internal bump:` lines
- idempotence: on a clean in-sync tree `git diff --quiet -- integration_tests/Cargo.lock`
  succeeds, so the commit step prints "already in sync" and exits 0 without an empty commit

`actionlint .github/workflows/release-plz.yml` exit **0**, and with
`-shellcheck=shellcheck` exit **0**. Tree restored after every simulation.

#### Carried forward

- **Residual, documented not fixed:** a third-party crate `[patch]`ed to a path would become
  source-less and silently inherit the internal-crate exemption. Noted in the workflow comment.
- Phase 2 must land the `integration_tests` `--locked` guard in the **`integration`** job (see
  the repo map above), and must not land before this phase, or CI breaks on the release PR.
- Operator note for the release PR: the sync push moves the head SHA, creating a **second**
  `action_required` run. The job emits a `::notice` naming the SHA that needs approval (S7).

#### Shipped

Pushed `ci/sync-integration-tests-lockfile`, opened **PR #155**, merged on green
(14/14 required contexts) as **`a7b6efc`** on 2026-09-06. The branch commit was
`62c76bf` after rebase; the squash-merge commit on `main` is `a7b6efc`, which is the
durable reference — the pre-squash shas are unreachable.

### Phase 2 — extend the CI lockfile guard

**Status: DONE.** Branch `ci/guard-integration-tests-lockfile` (round 1 shipped `8726392`;
a fresh **Opus** verifier returned **GAPS**; round 2 amended into `c7ab86f`; rebased onto
Phase 1 and given a round-3 wording correction). The pre-squash shas are unreachable once
merged, so the durable reference is the squash commit on `main` — recorded in Phase 3's
checkpoint, which merges after this one.
Files touched: `.github/workflows/ci.yml`, `.github/dependabot.yml`, and — folded in at
push time — this file plus the plan's Phase 1/2 status lines.

#### What shipped

A `cargo metadata --locked --manifest-path integration_tests/Cargo.toml --format-version 1`
step named "Verify integration_tests/Cargo.lock is consistent with its manifest", added to
the **`integration`** job (not `check`), sited immediately after protoc install and **before**
`cargo build`, so a stale lock fails fast rather than after an expensive compile.

The root guard's comment in `check` was rewritten in the same commit. It previously claimed
the guard was root-only and that guarding the second lock "would turn every release PR red" —
both false once Phase 1's `sync-integration-lock` job landed. The root guard **step** is
unchanged.

Round 2 additionally corrected two documentation inaccuracies (below).

#### Round 1 verdict: GAPS — all six acceptance criteria PASSED

The verifier confirmed the workflow change itself is correct and must not be redesigned:

- All **6 acceptance criteria passed**.
- **Siting in `integration` confirmed correct.** That job is itself a REQUIRED status context
  on `main` and carries no `if:` condition and no `paths` filter, so the guard runs on every
  PR and genuinely blocks merge — it is not a weaker position than `check`. The MSRV argument
  for keeping it out of `check` (that job pins RUSTUP_TOOLCHAIN 1.89.0 while
  `integration_tests/Cargo.toml` declares no `rust-version`) holds.
- **Firing test reproduced.** Adding `base64 = "0.22"` to `crates/macp-core/Cargo.toml` made
  the new step fail with **exit 101**. The guard actually fires.

Every gap returned was **documentation accuracy**, not behaviour:

| ID | Gap | Disposition |
|----|-----|-------------|
| 1 | The `ci.yml` root-guard comment (~L87-92) references `sync-integration-lock` in the present tense, which is false until Phase 1's PR #155 lands | **Not fixed by design.** The orchestrator merges #155 first and rebases this branch onto it, at which point the comment is true. Softening the tense would have to be un-done. |
| 2 | Merge ordering: this branch must not reach `main` before Phase 1 | **Not a code fix.** Handled by the orchestrator sequencing #155 ahead of this PR. |
| 3 | **The real one.** The new constraint is far wider than any comment or plan text stated. `integration_tests/Cargo.lock` records the seven path crates' full dependency edges, so a dependency change to **ANY** root-workspace crate reds the `integration` job unless that lock is regenerated in the same PR. The shipped comment said "a manifest edit committed without the regenerated lock", which reads as *the integration_tests manifest only* and under-states the rule | **Closed in round 2.** |
| 4 | `.github/dependabot.yml` misnamed the guarding job. Both cargo blocks ended with "the `cargo metadata --locked` step in ci.yml's Check job fails any PR whose lockfile disagrees with the manifests" — true for the root block, now wrong for `directory: "/integration_tests"`, whose guard lives in `integration` | **Closed in round 2.** |

#### Round 2 — gaps closed

- **Gap 3.** The `integration` job's comment now carries an explicit `SCOPE, wider than it
  looks:` paragraph stating that adding, removing or changing a dependency in **any**
  workspace crate — `crates/macp-*/Cargo.toml` or the root `Cargo.toml`, not just
  `integration_tests/Cargo.toml` — reds this job unless the lock is regenerated in the SAME
  PR, plus the one-line fix:
  `cargo metadata --manifest-path integration_tests/Cargo.toml --format-version 1 > /dev/null`
- **Gap 4.** The `/integration_tests` dependabot block's comment now names the `integration`
  job and states that Check guards only the root lock. The root (`directory: "/"`) block is
  untouched — its reference to the Check job is correct.
- The commit message gained a paragraph recording the wider constraint and the dependabot
  correction.

#### Pre-merge correction (round 3, orchestrator)

Phase 3's verifier caught that the `SCOPE` comment shipped here over-stated the rule:
"adding, removing or **changing** a dependency ... leaves that lock stale" is not true of a
requirement change the existing pin still satisfies. Counter-example in this very tree — the
root `Cargo.toml` requires `serde = "1"`, the root lock pins `1.0.229`, `integration_tests/
Cargo.lock` pins `1.0.228`, and `--locked` passes on both. Corrected in `ci.yml` before this
branch was pushed, so the comment and Phase 3's contributor docs state the same rule.
`actionlint` re-run: exit 0.

#### Round 2 verification (local)

| Check | Result |
|-------|--------|
| `actionlint .github/workflows/ci.yml` | exit **0**, no output |
| `actionlint -shellcheck=shellcheck .github/workflows/ci.yml` | exit **0**, no output |
| `python3 -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` | parses; `version: 2`; **no `versioning-strategy` key on any of the four update blocks** (PR #151's fix is intact and was not undone) |
| Root guard: `RUSTC_WRAPPER="" cargo metadata --locked --format-version 1` | exit **0** |
| Integration guard: `RUSTC_WRAPPER="" cargo metadata --locked --manifest-path integration_tests/Cargo.toml --format-version 1` | exit **0** |

Round 2 was executed in a throwaway `git worktree` off `ci/guard-integration-tests-lockfile`
so the main checkout (on `ci/sync-integration-tests-lockfile`, with this PROGRESS file
uncommitted) was never disturbed; the worktree was removed and pruned afterwards.

#### Carried forward

- **Merge ordering is a hard constraint.** Phase 1's PR #155 (`sync-integration-lock` in
  `release-plz.yml`) must merge **first**, and this branch must then be rebased onto it.
  Landing this guard alone would red the next release PR, and would also leave the round-1
  `ci.yml` comment referencing a job that does not exist.
- Reverting this guard without also reverting the Phase 1 sync is not a supported rollback:
  the guard is what keeps that lock's staleness off `main`. Revert both or neither.
- Phase 3's contributor documentation must state the **wide** rule (any workspace crate's
  dependency change requires regenerating the second lock), matching the comment shipped here
  — not the narrower "edit `integration_tests/Cargo.toml`" framing.

### Phase 3 — document the invariant in a tracked file

**Status: DONE.** Branch `docs/integration-tests-lockfile-rule` (round 1 `32a0fb1`, a fresh
**Opus** verifier returned **GAPS**, round 2 amended into `6b0e3ca` and returned **PASS**;
rebased onto Phase 2 and merged as `95ea207`, PR #157). Files: `CONTRIBUTING.md`, `docs/testing.md`, and
this plan. `CLAUDE.md` got the same paragraph as a **local mirror only** — it is gitignored
(`.gitignore:20`, `git ls-files CLAUDE.md` empty), so that edit is deliberately absent from
the PR diff. It was not force-added.

#### What shipped

- `CONTRIBUTING.md`: a new `## Lockfiles` section stating the rule and naming both guards by
  their CI display names, plus an `### Approving a release PR` subsection covering the
  two-`action_required`-runs trap that Phase 1's sync job creates.
- `docs/testing.md`: the same rule in the integration-suite section, cross-linked rather
  than duplicated.
- The standing "check the lock for staleness before pushing" chore is retired. The
  historical checklists in `plans/list-sessions-pagination*.md` are left as written —
  rewriting a finished plan's record would falsify it.

#### Round 1 verdict: GAPS — 1 blocker, 3 should-fix, 3 nits

The blocker was **merge ordering**, not content: all three branches were siblings off
`b150bea`, and landing Phase 3 first would have put two statements on `main` that were false
there — that CI guards the second lock, and that the manual chore is retired. Without Phase
1's sync job the lock is stale by construction after every bump, so the second is actively
misleading. Handled by sequencing (#155 -> #156 -> this), not by editing the text.

The substantive finds were factual errors in files this phase touched:

| ID | Gap | Disposition |
|----|-----|-------------|
| 2 | The plan cited Phase 1 as `d24d7dc`, orphaned by a rebase | Closed — now the squash commit `a7b6efc` (PR #155), which is the reference that survives |
| 3 | The plan claimed Phase 2 landed at `c7ab86f`, a commit that existed only on an unpushed local branch | Closed — now `c9650df` (PR #156), filled in after that PR merged |
| 4 | `CONTRIBUTING.md` said `cargo audit` "runs as advisory". It is **blocking** — the `audit` job carries no `continue-on-error` on the job or any of its 4 steps, and `docs/testing.md` already said so | Closed. Pre-existing error, but this phase appended a subsection directly beneath it |
| 4b | The same paragraph cited `plans/IMPROVEMENT_PLAN.md` and `plans/current/`, neither of which exists — `plans/BUILD_STATUS.md` itself records they were deleted 2026-07-06 | Closed — rewritten to the real `plans/` layout |
| 5 | Both docs said "adding, removing, or **changing** a dependency" invalidates the lock. Over-broad: a requirement change the existing pin still satisfies does not. This tree proves it — root lock `serde 1.0.229`, integration lock `1.0.228`, same `serde = "1"` requirement, `--locked` passes | Closed in the docs **and** in Phase 2's shipped `ci.yml` comment before that PR was pushed, so the two cannot contradict each other on `main` |
| 6 | No guidance for a dependabot root-workspace PR, which edits manifests and reds the new guard while dependabot's separate `/integration_tests` PR cannot fix it | Closed |
| 7 | "Approve the run at the SHA the sync job reports" never said how | Closed — `gh run list --commit <sha>` to find it, Actions UI "Approve and run" to approve. No `gh` approval subcommand was invented; none exists |

#### Round 2 verdict: PASS

All six re-checked and closed, with the verifier re-deriving the facts rather than trusting
the fixer: `gh run list --help` confirms `--commit` is real; every path newly named in the
`plans/` paragraph was confirmed to exist; the branch-protection claim ("12 contexts,
`strict: true`") was confirmed live against the API rather than carried over from an earlier
assertion in this session. Diff confined to the 3 intended files; fences balanced; the
`../CONTRIBUTING.md` link resolves.

#### Carried forward

- `.cargo/audit.toml`'s ignore list is currently **empty**, so "ignore list in
  `.cargo/audit.toml`" names the mechanism, not an active suppression. Matches how `ci.yml`
  describes itself; flagged because it can read as "we suppress advisories" at a glance.
- Merging #156 initially failed on "refusing to allow an OAuth App to create or update
  workflow ... without `workflow` scope" — **not** a CI failure. Dependabot's #154 landed
  meanwhile and touched three workflow files, leaving #156 `BEHIND`; under `strict: true` the
  merge tried to update the branch first, and that update pushes workflow files through the
  API. Resolved by merging `origin/main` into the branch locally and pushing over SSH, which
  is not subject to the OAuth App restriction. If it recurs on a behind branch:
  `gh auth refresh -h github.com -s workflow`.
